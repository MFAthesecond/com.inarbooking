/*
package stepdefinitions.hotelstepdefinitions;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import stepdefinitions.BaseStep;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
public class EndToEndTest extends BaseStep {
    private static final Logger LOGGER = LogManager.getLogger(HotelYoursDetailsSteps.class);

    @When("Click on the done button")
    public void click_on_the_done_button() {
        PAGES.getHotelPages().getHotelPage().
    }

}
*/
